---
apiVersion: v1
kind: Service
metadata:
  name: portal
  labels:
    app.kubernetes.io/name: portal
    app.kubernetes.io/component: app
    app.kubernetes.io/part-of: e2e-fixes
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 9898
      protocol: TCP
  selector:
    app.kubernetes.io/name: portal
