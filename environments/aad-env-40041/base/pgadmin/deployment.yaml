---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgadmin
  labels:
    app.kubernetes.io/name: pgadmin
    app.kubernetes.io/component: app
    app.kubernetes.io/part-of: env-postgres---kafka-mrtc7f4j
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pgadmin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pgadmin
        app.kubernetes.io/component: app
        app.kubernetes.io/part-of: env-postgres---kafka-mrtc7f4j
    spec:
      containers:
        - name: pgadmin
          image: pgadmin:placeholder
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 190m
              memory: 192Mi
            limits:
              cpu: 380m
              memory: 384Mi
          securityContext:
            allowPrivilegeEscalation: false
