---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: app
    app.kubernetes.io/part-of: env-portal---banco--wizard--mqu6tzcr
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 6379
      protocol: TCP
  selector:
    app.kubernetes.io/name: redis
