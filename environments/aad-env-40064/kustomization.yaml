---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aad-gitopsenv-40064

resources:
  - namespace.yaml
  - base
  - vault-auth.yaml
  - vault-secret-db.yaml

labels:
  - includeSelectors: false
    pairs:
      environment: "dev"
      managed-by: "aad"

images:
  - name: db
    newName: postgres
    newTag: "16"

replicas:
  - name: db
    count: 1
